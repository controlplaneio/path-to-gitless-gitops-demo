---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: a-simple-app
  namespace: apps
spec:
  interval: 10m
  url: oci://registry.iximiuz.com/oci-deploy
  ref:
    tag: v1.0
    digest: ${DIGEST}
  verify:
    provider: cosign
    secretRef:
      name: cosign-public-key